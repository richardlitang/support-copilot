# Integration Installation Doc

## Installation permissions

Installing the CRM integration requires admin access in both the product workspace and the CRM account.

## Redirect URL requirement

The integration setup page fails validation if the redirect URL does not exactly match the URL configured in the CRM developer console.

## Connection test

After installation, run the built-in connection test. A passing connection test confirms credentials but does not verify plan entitlement for every downstream feature.
