kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: support-copilot-local
nodes:
  - role: control-plane
    extraPortMappings:
      # host :8080 -> NodePort :30080 (web service)
      - containerPort: 30080
        hostPort: 8080
        protocol: TCP
